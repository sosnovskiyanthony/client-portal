{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "deploy": {
    "inputs": ["..."],
    "aptPackages": ["curl", "libatomic1"],
    "startCommand": "sh scripts/start-with-tailscale.sh"
  }
}
